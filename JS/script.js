$(function () {
  /* вписываем класс родителя */
  $(".portfolio__slider").slick({
    /* Перечисляем свойства slick слайдера */
    arrows: false,
    dots: true,
    autoplay: true,
    fade: true,
  });
});
